#include <stdio.h>
#include "main.h"
/**
 * _strlen - print the number of character of a word
 * @s: string to print the value of length
 */
int _strlen(char *s)
{
int i = 0;
for (i = 0; s[i] != '\0'; i++)
{
}
return (i);
}
